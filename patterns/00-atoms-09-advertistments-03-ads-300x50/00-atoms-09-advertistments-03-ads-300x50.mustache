&lt;script&gt;


    var tgx_ad_callback_ad_300x50{{#pos}}_{{pos}}{{/pos}} = function (event, sizes, breakpoint, oldBreakpoint) {
        if (event === 'render' &amp;&amp; this.id == 'ad-300x50{{#pos}}-{{pos}}{{/pos}}') {

        }
        else if (event === 'remove') {

        }
        else if (event === 'rendered') {

        }
    };

    tgx_ad_slots.push(function(ads){

        ads.addSlot({
            id: 'ad-300x50{{#pos}}-{{pos}}{{/pos}}',
            responsive: {
                sm: {sizes: ['300x50','320x50'], callback: tgx_ad_callback_ad_300x50{{#pos}}_{{pos}}{{/pos}} }
            },
            {{#exclusive}}exclusive: {{exclusive}},{{/exclusive}}
            {{#pos}}position: {{pos}},{{/pos}}{{^pos}}position: 1,{{/pos}}
            {{#defer}}defer: {{defer}}{{/defer}}{{^defer}}defer: 150{{/defer}}
        });

    });

&lt;/script&gt;